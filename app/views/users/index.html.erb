<%= link_to "投稿画面へ", new_user_path %>
<% @users.each do |user| %>
  <p>
    <%= user.name %>
    <%= user.age %>
    <%= link_to "詳細", user_path(user.id) %>
    <%= link_to "編集", edit_user_path(user) %>
  </p>
<% end %>